//criar uma variavel qty, ela deve ter a quantidade de elementos do array e outra variavel 'el' contendo o valor do terceiro elemento

const arr = ['a', 'b', 'c', 'd', 'e'];

let qty = arr.length;
let el = arr[3];

console.log(qty);
console.log(el);